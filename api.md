`clone user/repo`
- select the host
- select a workspace
- clone the repo

`clone user/repo -w workspace`
- select host
- clone the repo

`clone user/repo -p path`
- select host
- clone the repo

`clone user/repo -w workspace -h host`
- clone the repo

`clone user/repo -w workspace -p path`
- take the path precedence
- clone the repo
